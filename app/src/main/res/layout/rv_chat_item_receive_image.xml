<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/image_tv_receive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="4dp"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/image_iv_receive"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="name" />


    <ImageView
        android:id="@+id/image_iv_receive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="4dp"
        android:maxWidth="200dp"
        android:maxHeight="400dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/image_tv_receive"
        tools:src="@drawable/bg_barista" />

    <TextView
        android:id="@+id/message_receive_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/spoqa_han_sans_neo_regular"
        android:lineSpacingExtra="6sp"
        android:textColorHint="@color/chat_time_color"
        android:textSize="@dimen/font_size_super_small"
        app:layout_constraintBottom_toBottomOf="@id/image_iv_receive"
        app:layout_constraintStart_toEndOf="@id/image_iv_receive"
        tools:text="오후 1:00" />

</androidx.constraintlayout.widget.ConstraintLayout>